
version: 2


sources: 
  - name: staging
    database: dtc-de-jbvds 
    schema: trips_data_all

    tables:
      # We can also define a treshold for freshness here - this can be used to be sure data is up to data
      - name: green_tripdata
      - name: yellow_tripdata

# models:
#     - name: my_first_dbt_model
#       description: "A starter dbt model"
#       columns:
#           - name: id
#             description: "The primary key for this table"
#             tests:
#                 - unique
#                 - not_null

#     - name: my_second_dbt_model
#       description: "A starter dbt model"
#       columns:
#           - name: id
#             description: "The primary key for this table"
#             tests:
#                 - unique
#                 - not_null